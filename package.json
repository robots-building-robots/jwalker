{
  "name": "jwalker",
  "version": "1.0.0",
  "description": "Drop-in HTML5 canvas background effect — draws a decorative X behind .jwalker containers",
  "main": "index.js",
  "scripts": {
    "build": "esbuild jwalker.js --bundle --minify --outfile=jwalker.min.js"
  },
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.27.3"
  }
}
